import styled from "styled-components";

const Container = styled.div`
  width: 100%;
  height: 100vh;
  background: var(--dark);
  color: var(--light);
`;
const SkillPart = () => {
  return <Container>SkillPart</Container>;
};

export default SkillPart;
